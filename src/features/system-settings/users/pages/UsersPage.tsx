import { SettingsPlaceholderPage } from '../../shared/SettingsPlaceholderPage';

export function UsersPage() {
  return <SettingsPlaceholderPage title="用户管理" description="管理用户、角色归属、账号状态与最近登录记录。" />;
}
